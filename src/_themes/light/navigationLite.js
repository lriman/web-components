import colors from '../colors';

export default {
  borderColor: colors.brand.primary.medium,
  sectionTitleColors: {
    default: colors.base.onBase.disabled,
    hovered: colors.brand.primary.medium,
    active: colors.white,
  },
  linkColors: {
    default: colors.base.onBase.disabled,
    hovered: colors.brand.primary.medium,
    active: colors.white,
  },
  linkMarkColors: {
    default: colors.transparent,
    hovered: colors.transparent,
    active: colors.brand.primary.medium,
  },
};
