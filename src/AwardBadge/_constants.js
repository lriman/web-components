import { INPUT_SIZES } from '../IconNotification/_constants';

export const COMPONENTS = {
  GENERAL: 'AwardBadge',
};

export const REFS = {
  TYPES: {
    PRIMARY: 'primary',
    SECONDARY: 'secondary',
    NEGATIVE: 'negative',
  },
  SIZES: INPUT_SIZES,
};
