import colors from '../colors';

export default {
  gameSearches: {
    borderTopColor: colors.base.background.surface1,
    searchRule: {
      background: colors.base.background.surface1,
      boxShadow: `0px 2px 16px rgba(0, 0, 0, 0.5)`,
    },
  },
};
