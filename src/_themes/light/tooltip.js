import colors from '../colors';

export default {
  box: {
    color: colors.grey5,
    backgroundColor: colors.base.background.deep,
    padding: [8, 15],
    borderRadius: 4,
    boxShadow: `16px 32px 50px ${colors.base.transparent.dark70}`,
  },
};
