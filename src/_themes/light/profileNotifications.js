import colors from '../colors';

export default {
  actionColors: {
    normal: colors.base.onBase.low,
    hovered: colors.base.onBase.main,
  },
  dotBackgroundColor: colors.brand.primary.light,
  notViewedItemBackgroundColor: colors.brand.primary.deep,
};
